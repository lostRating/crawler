package server;

import socket.BaseConnector;

import socket.EventSender;
import event.*;
import common.Config;
import web.Client;

public class test {
	public static void main(String args[]) {
		//String url = "https://en.wikipedia.org/wiki/John";
		//String page = Client.getPage(url);//.substring(0, 100);
		//System.out.println(page);
		//new BaseConnector("127.0.0.1", Config.clientPort, sender, 5000, 1).start();
	}
}
